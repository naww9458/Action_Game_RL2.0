# Thin index: maps control-policy version id -> on-disk config under models/<version>/.
versions:
  V1:
    robot_pattern: unitree_g1
    config: models/V1/control_policy.yaml
  boxing_v1:
    robot_pattern: unitree_g1
    config: models/boxing_v1/control_policy.yaml
